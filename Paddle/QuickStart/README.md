# 假设我们希望用神经网络模型去拟合x在[-PI, PI]区间内，u=sin(x)这一函数。
# 在拟合函数已知和未知两种情形下，如何去尽可能地准确拟合。
#
# 第一种场景下，假设已知目标函数的解析解就是u=sin(x)，我们采用监督训练的思路，
# 直接用该公式生成标签因变量u，与自变量x共同作为监督数据对模型进行训练。

# 第二种场景下，假设不知道目标函数u的解析解，但我们知道其满足某种微分关系，
# 我们这里以其中一个满足条件的微分方程 u对x的偏导是cos(x)为例，介绍如何生成数据进行训练。